import SavedCandidatesPageContent from "./saved-candidates";

export default async function SavedCandidatesPage() {
  // Fetch data here using Supabase (server-side)
  // Example:
  // const candidates = await fetchCandidatesForEmployer();

  return <SavedCandidatesPageContent /* candidates={candidates} */ />;
}
